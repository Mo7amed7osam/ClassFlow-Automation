<UserControl x:Class="ZoomAutoAdmit.WindowsUI.Views.RecordingsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
<UserControl.Resources><BooleanToVisibilityConverter x:Key="Visible"/></UserControl.Resources>
<!-- The page binds to MainViewModel.RecordingsDashboard, like the other pages bind to their part of the window's model. -->
<Grid x:Name="Root" DataContext="{Binding RecordingsDashboard}">
<Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

<!-- Status and actions -->
<Border Style="{DynamicResource Card}" Padding="16,12" Margin="0,0,0,12"><Grid>
<Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
<StackPanel Orientation="Horizontal" VerticalAlignment="Center">
<Ellipse x:Name="StatusDot" Width="10" Height="10" Margin="0,0,10,0" VerticalAlignment="Center">
<Ellipse.Style><Style TargetType="Ellipse"><Setter Property="Fill" Value="{DynamicResource ErrorBrush}"/><Style.Triggers>
<DataTrigger Binding="{Binding StatusTone}" Value="good"><Setter Property="Fill" Value="{DynamicResource SuccessBrush}"/></DataTrigger>
<DataTrigger Binding="{Binding StatusTone}" Value="busy"><Setter Property="Fill" Value="{DynamicResource WarningBrush}"/></DataTrigger>
</Style.Triggers></Style></Ellipse.Style></Ellipse>
<StackPanel VerticalAlignment="Center">
<TextBlock FontWeight="SemiBold" AutomationProperties.LiveSetting="Polite"><Run Text="{Binding StatusText, Mode=OneWay}"/><Run Text="   "/><Run Text="{Binding AgentText, Mode=OneWay}" Foreground="{DynamicResource MutedBrush}" FontWeight="Normal"/></TextBlock>
<TextBlock Text="{Binding StatusDetail}" Style="{DynamicResource Muted}" FontSize="12" Margin="0,3,0,0" TextWrapping="Wrap"/>
</StackPanel></StackPanel>
<WrapPanel Grid.Column="1" VerticalAlignment="Center">
<Button Content="Start server" Command="{Binding StartCommand}" Visibility="{Binding IsServerMode, Converter={StaticResource Visible}}" Margin="4,0" AutomationProperties.Name="Start the server"/>
<Button Content="Stop" Command="{Binding StopCommand}" Visibility="{Binding IsServerMode, Converter={StaticResource Visible}}" Margin="4,0" AutomationProperties.Name="Stop the server"/>
</WrapPanel></Grid></Border>

<!-- Connection settings (shown on demand, and at first until they are complete) -->
<Border Grid.Row="1" Style="{DynamicResource Card}" Margin="0,0,0,12"><StackPanel MaxWidth="900" HorizontalAlignment="Left">
<TextBlock Text="Connection" Style="{DynamicResource CardTitle}"/>
<TextBlock Text="On the server PC the app runs the backend and the agent itself. On a coordinator's PC it only shows the server's dashboard; they sign in with the account the admin made for them." Style="{DynamicResource Muted}" TextWrapping="Wrap" Margin="0,0,0,12"/>
<StackPanel Orientation="Horizontal" Margin="0,0,0,12">
<RadioButton Content="This PC is the server" IsChecked="{Binding IsServerMode}" GroupName="DashboardMode" Margin="0,0,24,0"/>
<RadioButton Content="Connect to a server" IsChecked="{Binding IsClientMode}" GroupName="DashboardMode"/>
</StackPanel>

<StackPanel Visibility="{Binding IsClientMode, Converter={StaticResource Visible}}">
<TextBlock Text="Server address" Style="{DynamicResource FieldLabel}"/>
<TextBox Text="{Binding ServerUrl, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,5,0,4" AutomationProperties.Name="Server address"/>
<TextBlock Text="The https:// address the admin gave you, e.g. https://their-pc.example.ts.net" Style="{DynamicResource Muted}" FontSize="12" Margin="0,0,0,12"/>
</StackPanel>

<StackPanel Visibility="{Binding IsServerMode, Converter={StaticResource Visible}}">
<TextBlock Text="Backend folder" Style="{DynamicResource FieldLabel}"/>
<TextBox Text="{Binding BackendFolder, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,5,0,10" AutomationProperties.Name="Backend folder" FontFamily="Consolas"/>
<Grid Margin="0,0,0,10"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
<StackPanel Margin="0,0,8,0"><TextBlock Text="Database host" Style="{DynamicResource FieldLabel}"/><TextBox Text="{Binding DatabaseHost}" Padding="10" Margin="0,5,0,0" AutomationProperties.Name="Database host"/></StackPanel>
<StackPanel Grid.Column="1" Margin="0,0,8,0"><TextBlock Text="Port" Style="{DynamicResource FieldLabel}"/><TextBox Text="{Binding DatabasePort}" Padding="10" Margin="0,5,0,0" AutomationProperties.Name="Database port"/></StackPanel>
<StackPanel Grid.Column="2" Margin="0,0,8,0"><TextBlock Text="User" Style="{DynamicResource FieldLabel}"/><TextBox Text="{Binding DatabaseUser}" Padding="10" Margin="0,5,0,0" AutomationProperties.Name="Database user"/></StackPanel>
<StackPanel Grid.Column="3" Margin="0,0,8,0"><TextBlock Text="Database" Style="{DynamicResource FieldLabel}"/><TextBox Text="{Binding DatabaseName}" Padding="10" Margin="0,5,0,0" AutomationProperties.Name="Database name"/></StackPanel>
<StackPanel Grid.Column="4"><TextBlock Text="Backend port" Style="{DynamicResource FieldLabel}"/><TextBox Text="{Binding Port}" Padding="10" Margin="0,5,0,0" AutomationProperties.Name="Backend port"/></StackPanel>
</Grid>
<TextBlock Text="Database password" Style="{DynamicResource FieldLabel}"/>
<StackPanel Orientation="Horizontal" Margin="0,5,0,4">
<!-- Read once by the code-behind and cleared; a binding would keep it in memory. -->
<PasswordBox x:Name="DatabasePassword" Width="280" Padding="10" MaxLength="200" AutomationProperties.Name="Database password"/>
<Button Content="Save password" Click="SavePassword" Margin="8,0,0,0"/>
<Button Content="Forget" Command="{Binding ForgetPasswordCommand}" Margin="6,0,0,0"/>
</StackPanel>
<TextBlock Text="{Binding PasswordState}" Style="{DynamicResource Muted}" FontSize="12" Margin="0,0,0,10"/>
<CheckBox Content="Start the server and the agent when the app opens (they stop when it closes)" IsChecked="{Binding StartWithApp}" Margin="0,0,0,12"/>
</StackPanel>

<StackPanel Orientation="Horizontal">
<Button Content="Save" Style="{DynamicResource PrimaryButton}" Command="{Binding SaveCommand}" Padding="18,8"/>
<TextBlock Text="{Binding Notice}" Style="{DynamicResource Muted}" VerticalAlignment="Center" Margin="14,0,0,0" AutomationProperties.LiveSetting="Polite"/>
</StackPanel>
</StackPanel></Border>

<!-- The web dashboard is retired: everything is a page of the app now (Sessions, Recordings, Coordinators & groups). -->
<Border Grid.Row="2" Style="{DynamicResource Card}" Padding="0" ClipToBounds="True" Visibility="Collapsed"><Grid>
<wv2:WebView2 x:Name="Browser" AutomationProperties.Name="Recordings dashboard"/>
<Border x:Name="Placeholder" Background="{DynamicResource CardBrush}">
<StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
<TextBlock x:Name="PlaceholderTitle" Text="The dashboard is not available yet" FontSize="18" FontWeight="SemiBold" TextAlignment="Center"/>
<TextBlock x:Name="PlaceholderText" Text="Start the server, or set the connection above." Style="{DynamicResource Muted}" TextAlignment="Center" TextWrapping="Wrap" Margin="0,8,0,0"/>
</StackPanel></Border>
</Grid></Border>
</Grid>
</UserControl>
