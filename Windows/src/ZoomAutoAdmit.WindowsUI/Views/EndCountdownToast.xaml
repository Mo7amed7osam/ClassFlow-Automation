<Window x:Class="ZoomAutoAdmit.WindowsUI.Views.EndCountdownToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False" ShowActivated="False"
        Topmost="True" SizeToContent="Height" Width="400" ResizeMode="NoResize" Title="Zoom Auto Admit">
  <Border CornerRadius="14" Padding="16,14" Margin="10" Background="#F00E1523" BorderBrush="#24FFFFFF" BorderThickness="1">
    <Border.Effect><DropShadowEffect Color="#000000" Opacity="0.45" BlurRadius="24" ShadowDepth="6" Direction="270"/></Border.Effect>
    <DockPanel>
      <Border DockPanel.Dock="Left" Width="4" CornerRadius="2" Background="#F5A524" Margin="0,2,12,2"/>
      <StackPanel>
        <TextBlock x:Name="TitleText" Foreground="#E8EDF7" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
        <TextBlock x:Name="MessageText" Foreground="#9AA6BD" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"/>
        <Border Height="4" CornerRadius="2" Background="#1FFFFFFF" Margin="0,10,0,0" HorizontalAlignment="Stretch">
          <Border x:Name="Bar" CornerRadius="2" Background="#F5A524" HorizontalAlignment="Left" Width="0"/>
        </Border>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
          <Button x:Name="ManualButton" Content="I'll end it myself" Click="EndManuallyClicked" Cursor="Hand"
                  Foreground="#C9D4E8" Background="#1FFFFFFF" BorderThickness="0" Padding="12,6" FontSize="12" Margin="0,0,8,0"/>
          <Button x:Name="NowButton" Content="End now" Click="EndNowClicked" Cursor="Hand"
                  Foreground="#0E1523" Background="#F5A524" BorderThickness="0" Padding="14,6" FontSize="12" FontWeight="Bold"/>
        </StackPanel>
      </StackPanel>
    </DockPanel>
  </Border>
</Window>
