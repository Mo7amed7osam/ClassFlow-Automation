<Application x:Class="ZoomAutoAdmit.WindowsUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <!-- The old padding-only styles were removed: every window that shows controls merges
         Themes/Controls.xaml, which defines the real button, field and card styling. The one
         window that does not, the desktop toast, sets its own padding on the single button it
         has, so nothing here is needed any more. -->
</Application>
