.build/
dist/
.DS_Store

# Local IDE settings
.vscode/

# Local diagnostics and captures
*.log
*.jsonl

# Swift and macOS artifacts
*.dSYM/

# Dotnet and Visual Studio artifacts
[Bb]in/
[Oo]bj/
.vs/
TestResults/
*.user_uploaded/
.user_uploaded/


# Debug captures written while the app runs; regenerated, never source.
diagnostics/
# A local copy kept while switching branches; not part of the project.
_local-before-branch-restore/

# Secrets. api.env and api_key.env are committed on purpose for the cloud sessions and must be
# rotated once that work is done; see CLOUD.md. Every other env file stays local.
*.env
.env
!api.env
!api_key.env
!Backend/cloud.env

# WPF's temporary project copies made during a build
*_wpftmp.csproj

# Written by Scripts/cloud-setup.sh for Scripts/cloud-verify.sh to source; local to one machine.
.cloud-env
